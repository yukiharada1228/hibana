# Hibana core — 現行仕様

HibanaはWorkersに近い開発体験を持つセルフホスト型Wasm FaaSとする。実行の中核はWasmtime。Cloudflareサービス全体の再実装やWrangler互換を現行スコープに含めない。

## ワークロード

1. `wasi:http/incoming-handler@0.2.3`をexportするWebAssembly Component。
2. Hono/JavaScript/TypeScriptのFetch handler。CLI内部のフレームワーク非依存な変換層で上記HTTP Componentへ変換する。Honoはdefault exportのみで使う。

RustとGoにも同じHTTP契約のテンプレートを提供する。言語ごとのビルドはCLIで行い、サーバーはComponentを共通に検証・配備する。

WASI Preview 1モジュール、任意のWIT、ブラウザ向けWasm、Emscripten、Node.js全APIは未対応。言語名だけで動作を保証せず、Componentのimport/exportがホストの提供する契約に一致することを必要条件とする。

## 開発者向けの入口

`hibana init --template hono|javascript|rust|go`、`hibana dev`、`hibana build`、`hibana deploy`を提供する。認証とSecretsも同じCLIから扱う。設定は`hibana.json`。JSの`main`と既存Componentの`component`は排他で、後者には言語共通の`build.commands`・`build.watch`を指定できる。CLI自体はNode.js、JSコンパイラーは任意依存とする。SDKの詳細は[sdk/README.md](sdk/README.md)に記載する。

Control Planeは認証・テナント管理・Component登録・Wasm検証/保管・バージョン選択・実行受付を担当する。HTTP ingressは署名付き内部リクエストでWorkerへ転送する。非同期invoke、bytes handler、Canary、NATS配送とキュー量ベースのスケール制御は提供しない。アプリケーションの永続Storage/Queue Bindingは提供しない。

## 残す運用機能

環境変数と暗号化Secrets、deny-by-default capability、リソース制限、RLSテナント分離、API認証、署名付きジョブ/結果、バージョン切替/rollback、Prometheus/任意のOTel、Kubernetes配置を維持する。

## 旧構成からの変更

KV・R2・D1・Durable Objects・Queues・Cron・外部イベント・chain trigger・Wrangler APIとサーバー側JSコンパイラーを削除する。既存のアプリがこれらを使用している場合、そのままの再配備/実行はサポートしない。

適用済みSQLマイグレーション0001–0023は履歴として保持し、チェックサムを書き換えない。旧機能のテーブルやデータを自動DROPしない。旧機能のAPI・スケジューラは新コードでは起動しない。

アップグレード前に旧機能の受付を止め、実行中ジョブと旧スケジュールを停止・確認する。旧Bindingを含むWasmは、必要な機能を取り除いたソースから再ビルドする。管理されたクラスタでは廃止した`hibana-compiler`のDeployment/Service/PodDisruptionBudget/NetworkPolicyも削除する（Kustomizeのapplyだけでは旧リソースは消えない）。旧データの物理削除は別の明示的な保守作業とする。

MVPの判断基準は[docs/mvp.md](docs/mvp.md)を参照する。0024は新規HTTP実行を識別する列を追加し、旧非同期実行をHTTPとして再開しない。rollbackはコードの参照先のみを変更し、共有vars/Secretsは変更しない。
