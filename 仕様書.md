# WASM FaaS Platform Specification

## 0. 本書の用語規約

本書のキーワード「しなければならない (MUST)」「すべきである (SHOULD)」「してもよい (MAY)」「してはならない (MUST NOT)」は、RFC 2119 / RFC 8174 に従って解釈する。要件レベルはこれらの語で明示し、日本語の語感に依存しない。

スコープ（v1 で実装する範囲）は §14 のスコープ表で管理する。章の役割: §10 = 将来拡張の総覧、§13 = 外部 AI サービス統合、§14 = スコープのステータス管理。

---

## 1. 概要

本システムは Rust をベースとしたイベント駆動型 FaaS (Function as a Service) プラットフォームである。

ユーザーは WASI Component Model に準拠した WebAssembly Component をデプロイし、HTTP API またはイベント経由で実行できる。

本システムは以下を目的とする。

* 高速起動（Worker 側 Component キャッシュと事前コンパイルを前提とする。§3.6 / §3.5）
* サンドボックス実行（多層防御。§7）
* マルチテナント対応（テナント分離を全レイヤで強制する。§3.7）
* イベント駆動アーキテクチャ
* Component 単位のデプロイ
* 将来的な AI / Workflow 統合

LLM 推論機能は本仕様の対象外とし、将来的な外部サービス統合として扱う（§13）。

### 1.1 Function と Component の関係

本システムにおける「Function」は、1 つの WASI Component として実装される。以降、デプロイ・実行の最小単位は **Component** と呼ぶ。

---

## 2. システムアーキテクチャ

論理構成（結果返却経路を含む）。物理／デプロイ構成は `アーキテクチャ図.md` および `Kubernetes上に複数のWasmtime Workerを並べる構成.md` を参照（正典は本書本文とする）。

```text
Client
   │  (1) HTTP API（要認証）
   ▼
Axum (Control Plane)  ── x N（Ingress/LB で水平分散）
   │
   ├── PostgreSQL（メタデータ・状態・実行履歴）
   │
   ├── Shared Store（Redis 等。レート制限 / 同時実行 / login 失敗カウンタの x N 共有。§8）
   │
   ├── Object Storage（WASM Component 本体 + 大容量 I/O 退避）
   │      ▲ (3) Component 取得（cache miss 時のみ／read-only）
   │      └ (4b) 大出力の書き込み（execution 出力プレフィックス限定の短命 write 資格）
   │
   └── NATS JetStream（Job Queue / Event Bus）
            │  (2) Publish Job（tenant スコープ subject + ジョブ署名トークン）
            ▼
     Wasmtime Workers ── x N（共有 Pull Consumer = competing consumers で負荷分散）
            │
            ▼
     WASM Components（実行）
            │  (4a) 結果（小）・ログを Publish（ジョブ署名トークンを echo）
            ▼
        NATS JetStream
            │  (5) Axum が購読・出所検証・永続化
            ▼
        PostgreSQL ──► (6) Client が取得（ポーリング / SSE）
```

Control Plane と Data Plane を分離する（設計原則 1）。Control Plane は Component を直接実行してはならない (MUST NOT)。実行要求は必ず NATS JetStream 経由で Data Plane へ配送しなければならない (MUST)。

---

## 3. コンポーネント構成

### 3.1 Axum Control Plane

責務

* API 提供（§6）
* 認証・認可（§3.7）
* Component 管理（登録・検証・バージョン管理・削除）
* 実行要求受付（JetStream への Publish）
* 実行結果・ログの受信・出所検証・永続化
* 実行状態管理

Control Plane は Component を直接実行してはならない (MUST NOT)。実行要求は必ず NATS JetStream 経由で Data Plane へ配送しなければならない (MUST)。

水平スケール可能とし、複数インスタンス (Axum x N) は Ingress / Load Balancer で分散する。インスタンス間で状態を共有しないステートレス設計とし、状態はすべて PostgreSQL に集約する（設計原則 6）。

### 3.2 PostgreSQL

責務

* システム状態管理
* テナント / ユーザー / 権限管理
* Component メタデータ・バージョン管理
* 実行履歴・実行状態管理
* 監査ログ

全エンティティはテナントに属し、`tenant_id` を必須カラムとして持たなければならない (MUST)。アプリケーションクエリは必ず `tenant_id` でフィルタし、加えて Row Level Security (RLS) による多重防御を有効化しなければならない (MUST)。

RLS の運用（テナントコンテキストの安全な受け渡し）は次を満たさなければならない (MUST)。

* 全テナント表で `ENABLE ROW LEVEL SECURITY` かつ `FORCE ROW LEVEL SECURITY` を設定する。
* アプリケーション接続ロールは `BYPASSRLS` を持たない非特権ロールとする。
* リクエスト処理の冒頭、トランザクション内で `SET LOCAL app.tenant_id = '<tenant>'` を実行し、RLS ポリシーは `current_setting('app.tenant_id')` で照合する。`SET LOCAL` はトランザクション境界でリセットされるため、コネクションプール再利用時にテナント値が残存しない。

スキーマ（型・制約は実装時に詳細化するが、最低限以下を満たす）。

Tenants / Users

```sql
CREATE TABLE tenants (
  id          TEXT PRIMARY KEY,            -- 例: ten_xxx（推測困難なランダムID）
                                           -- NATS subject トークンとして使うため
                                           -- '.' '*' '>' 空白を含まない文字種に限定する（§3.3）
  slug        TEXT NOT NULL UNIQUE,        -- 人間可読なグローバル一意識別子（§6.0 login のテナント解決）
  name        TEXT NOT NULL,
  status      TEXT NOT NULL DEFAULT 'active', -- active | suspended
  quotas      JSONB NOT NULL DEFAULT '{}', -- テナント別クォータ上書き（§8。{} = グローバル既定）
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE users (
  id            TEXT PRIMARY KEY,          -- 例: usr_xxx
  tenant_id     TEXT NOT NULL REFERENCES tenants(id),
  email         TEXT NOT NULL,
  password_hash TEXT,                      -- argon2id 等で保存（§6.0 login。将来 OIDC 連携時は NULL 可）
  role          TEXT NOT NULL DEFAULT 'member', -- admin | member（権限差は §3.7）
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (tenant_id, email)
);

-- API トークン（API キー / JWT のメタデータ。実体はハッシュ保存）
CREATE TABLE api_tokens (
  id          TEXT PRIMARY KEY,            -- 例: tok_xxx
  tenant_id   TEXT NOT NULL REFERENCES tenants(id),
  user_id     TEXT REFERENCES users(id),  -- ユーザー紐付け（サービストークンは NULL 可）
  token_hash  TEXT NOT NULL,              -- 平文は保存しない
  scopes      TEXT[] NOT NULL,            -- read | invoke | deploy | admin（§3.7）
  expires_at  TIMESTAMPTZ,
  revoked_at  TIMESTAMPTZ,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

Components / Versions

```sql
CREATE TABLE components (
  id          TEXT PRIMARY KEY,            -- 例: cmp_xxx
  tenant_id   TEXT NOT NULL REFERENCES tenants(id),
  name        TEXT NOT NULL,               -- テナント内一意
  active_version_id TEXT,                  -- latest 解決 / ロールバック用の現行版（§6.7）
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
  deleted_at  TIMESTAMPTZ,                 -- soft delete
  UNIQUE (tenant_id, name)
);

CREATE TABLE component_versions (
  id            TEXT PRIMARY KEY,          -- 例: ver_xxx
  component_id  TEXT NOT NULL REFERENCES components(id),
  tenant_id     TEXT NOT NULL REFERENCES tenants(id),
  version       TEXT NOT NULL,             -- semver（不変）
  storage_uri   TEXT NOT NULL,             -- Object Storage 上の本体 URI
  wasm_sha256   TEXT NOT NULL,             -- 整合性・キャッシュキー
  size_bytes    BIGINT NOT NULL,
  capabilities  JSONB NOT NULL DEFAULT '{}', -- 付与 capability 宣言（§4.4。{} = deny-all）
  resource_limits JSONB NOT NULL,          -- §4.3
  status        TEXT NOT NULL DEFAULT 'pending', -- pending | active | deprecated
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (component_id, version)
);
```

Executions

```sql
CREATE TABLE executions (
  id              TEXT PRIMARY KEY,        -- 例: exec_xxx（サーバ生成・冪等の最終キー）
  tenant_id       TEXT NOT NULL REFERENCES tenants(id),
  component_id    TEXT NOT NULL REFERENCES components(id),
  version_id      TEXT NOT NULL REFERENCES component_versions(id),
  idempotency_key TEXT,                    -- クライアント提示（§6.3 / §6.6）
  status          TEXT NOT NULL DEFAULT 'pending',
                  -- pending | running | succeeded | failed | timeout
  input_ref       TEXT,                    -- 小さい入力はインライン、大きい場合は Storage URI
  output_ref      TEXT,                    -- 同上
  error           JSONB,                   -- 分類・メッセージ（§6.5）
  attempts        INT NOT NULL DEFAULT 0,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
  started_at      TIMESTAMPTZ,
  finished_at     TIMESTAMPTZ
);

CREATE UNIQUE INDEX ON executions (tenant_id, idempotency_key)
  WHERE idempotency_key IS NOT NULL;       -- テナント名前空間での冪等一意化
CREATE INDEX ON executions (tenant_id, created_at DESC);
CREATE INDEX ON executions (component_id, status);
```

監査ログ（§3.7 / §7）

```sql
CREATE TABLE audit_logs (
  id          BIGSERIAL PRIMARY KEY,       -- append-only
  tenant_id   TEXT NOT NULL,
  actor       TEXT,                        -- user_id / token_id
  action      TEXT NOT NULL,               -- auth_failed | authz_denied | capability_approved
                                           -- | component_deleted | token_issued | token_revoked ...
  target      TEXT,
  detail      JSONB,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

監査ログは追記のみ (append-only) とし、更新・削除を許してはならない (MUST NOT)。保持期間を定める。

状態遷移は `pending → running → (succeeded | failed | timeout)` とする。`pending → running` の遷移は条件付き UPDATE（CAS）で保護し、再配送による二重実行を防ぐ（§6.6）。

実行ログ・結果の保持期間・暗号化・マスキングは §3.8 / §7 に従う。

### 3.3 NATS JetStream

責務

* Job Queue（実行要求の配送）
* Event Bus（結果・ログ・失敗通知）
* Retry / Durable Delivery
* （必要な箇所に限った）Fan-out

#### テナント分離方式（確定）

本システムは **単一 account 内の subject 権限分離** を採用する（account 単位分離は採らない）。理由: Worker は汎用ランタイムであり、テナントごとに account / 接続を分けると全テナント Job を 1 つの共有 Pull Consumer（competing consumers）で捌けず、Worker の汎用性と両立しないため。

* 接続は TLS / mTLS を必須とする (MUST)。
* 接続認証は nkey / JWT で行い、ロール別に subject permission を付与する (MUST)。
  * **Control Plane**: `tenant.*.component.invoke` に publish 可、`tenant.*.component.result` / `.logs` / `.failed` を subscribe 可（`.failed` / DLQ を購読して execution を終端化するため。§6.6）。
  * **Worker**: `tenant.*.component.invoke` を subscribe 可、`tenant.*.component.result` / `.logs` / `.failed` に publish 可。
* JetStream はストリーム単位でテナント横断の耐久キューを構成し、可用性のためクラスタ構成（replica R ≥ 3）とする (MUST)。

#### 結果の出所認証（Worker 越権・結果偽装への対策）

単一 account 構成では Worker は広域の publish 権限を持つため、subject 権限のみでは「侵害された / 不正な Worker による他テナントへの偽結果注入」を防げない。これを補うため次を必須とする (MUST)。

* Control Plane は invoke メッセージにジョブ単位の **署名トークン**（execution_id・tenant_id・version_id・発行時刻・有効期限を含む署名付き値）を載せる。
* Worker は結果・ログを publish する際にこの署名トークンをそのまま echo する。Worker はトークンを検証も生成もせず、**不透明 (opaque) な値として echo するのみ**であり、検証鍵を保持してはならない (MUST NOT)。
* Axum は `.result` 受信時に、署名の正当性・execution_id とテナント / Component の整合・状態遷移（CAS）を検証してから永続化する。検証失敗は破棄し監査ログに記録する。

署名鍵のライフサイクル (MUST)

* 署名方式は **非対称署名 (Ed25519)** とする。署名は Control Plane が秘密鍵で行い、検証は公開鍵で行う（Axum は公開鍵のみで検証でき、Worker は鍵を持たない）。対称鍵を発行系と全 Axum で共有すると一箇所の漏洩で全テナント偽結果注入が可能になるため採らない。
* 鍵には `kid`（鍵 ID）を付与し、新旧鍵の overlap 期間を設けて無停止ローテーションする。秘密鍵は KMS / Secrets で管理し、§9 のプロビジョニングで配布する。

トークン有効期限と配送リトライの整合 (MUST)

* 署名トークンの有効期限は、正常な再配送（`ack_wait` 超過・backoff 後の再配送）で Worker が完了する時点でも失効しないよう設定する（§6.6）。固定 `ack_wait` 構成では `有効期限 ≥ ack_wait × max_deliver + 実行上限 + 余裕`。backoff 配列を用いる構成では最悪滞留が backoff に支配されるため `有効期限 ≥ Σ_{i=1..max_deliver} backoff[i] + 実行上限 + 余裕` とする。
* 万一トークンが失効していても、Axum は execution が `pending` / `running` であり署名内容（execution_id・tenant）が DB と整合すれば結果を受理する（有効期限切れのみを理由に正規の結果を破棄して実行喪失を招いてはならない (MUST NOT)）。有効期限は主にリプレイ窓を絞る目的で用いる。

Subject 体系（テナント名前空間を必須とする）

```text
tenant.{tenant_id}.component.invoke    -- 実行要求（Control Plane のみ publish 可）
tenant.{tenant_id}.component.result    -- 実行結果（Worker のみ publish 可）
tenant.{tenant_id}.component.failed    -- 失敗・DLQ 通知
tenant.{tenant_id}.component.logs      -- 実行ログ
```

#### Consumer セマンティクス

* `component.invoke` は **テナント別の lane Consumer（competing consumers）** で消費する（M8, §8）。テナントごとに `tenant.{id}.component.invoke` を filter する Durable Pull Consumer（lane）を置き、dedicated 枠を超えた分は**有界な overflow lane 1 本**へ束ねる。1 ジョブを 1 Worker のみが処理し ack で排他する。同一ジョブの多重実行は許されない (MUST NOT)。
* stream は **WorkQueue retention** とし、**どの subject もちょうど 1 つの Consumer に属する**ことをサーバ側で強制する（filter が重なる Consumer の作成が拒否されるため、多重配送が構成上あり得なくなる）。
* lane は **Control Plane が唯一の作成者**であり、enqueue 時の ensure と定期 reconcile（単一 writer）で冪等に用意する。したがってテナント動的追加時も**運用手順は不要**（自動化されている）。Worker は lane を作らず、購読対象を周期 discovery + 変更通知で解決する。
* ログ・通知など真に broadcast が必要なものだけを別 Consumer / Stream で Fan-out する。

すべての実行要求は JetStream を経由しなければならない (MUST)。

### 3.4 Object Storage

責務

* WASM Component 本体の保存。
* 大容量の実行入力 / 出力の退避（NATS メッセージ既定上限 1 MiB を超えるペイロード。§6.4）。

保存レイアウト（テナントプレフィックスで分離する）

```text
tenants/
  {tenant_id}/
    components/
      markdown/
        v1/component.wasm
    io/
      {execution_id}/
        input
        output
```

アクセス制御

* Control Plane は Component 本体を保存し、その `storage_uri` と `wasm_sha256` を PostgreSQL に記録する。
* **Component 本体の読み取り**: Worker への資格情報は、対象テナント / Component の**当該 version オブジェクトキーに限定**した読み取り専用かつ短命のもの（署名付き URL / STS）とする (MUST)。
* **大入力の読み取り**: 大入力（`input_ref`）がある場合、Control Plane は当該 execution の入力オブジェクトキー（`tenants/{tenant_id}/io/{execution_id}/input`）**のみ**に限定した短命 read-only 資格を Job に同梱する (MUST)。Worker はこの指定キー以外を読み取ってはならない (MUST NOT)。Control Plane は invoke 受付時に `input_ref` が `tenants/{呼び出しテナント}/io/{当該 invoke の execution_id}/input` に**完全一致**することを検証してから Job に載せなければならない (MUST)。prefix 一致では不十分であり、同一テナント内の別 execution / 別ユーザーの入力を指す `input_ref` を拒否する（input 側も output 側と同じ execution_id 単位の隔離とする）。Worker へ同梱する read 資格は invoke の execution_id から導出したキーに固定する。
* **大出力の書き込み（例外）**: Worker は原則 Object Storage へ書き込んではならない (MUST NOT)。ただし大出力退避のため、Control Plane は当該 execution の出力オブジェクトキー（`tenants/{tenant_id}/io/{execution_id}/output`）**のみ**に限定した短命 write 資格を Job に同梱してよい。Worker はこの指定キー以外へ書き込んではならない (MUST NOT)。同時に複数 Job を処理する Worker も、各資格は当該 execution_id のキーに限定されるため Job 間の交差書き込みは起きない。
* 短命資格の要件 (MUST): TTL 上限を定める（既定 5 分・上限 15 分）。スコープは prefix ではなく単一オブジェクトキーまで限定する。STS を用いる場合は revocation / condition を付与する。署名付き URL は個別失効ができないため、短 TTL かつ単一オブジェクト限定で発行する。
* バケットポリシーでテナントプレフィックスを跨ぐアクセスを禁止する。

### 3.5 Wasmtime Workers

責務

* Job 受信（共有 Pull Consumer = competing consumers）
* Component 取得（cache miss 時のみ Object Storage から read-only 取得。§3.6）
* Wasm 実行（1 リクエストにつき 1 Store / 1 Instance を使い捨て）
* Resource 制限の適用（§4.3）
* 結果・ログの返却（テナント subject へ publish、ジョブ署名トークンを echo。§3.3）

Worker は汎用ランタイムであり、特定の Component に固定されない。Job の指定する Component / Version を動的にロードして実行する。アーキテクチャ図の Worker→Component 矢印は例示であり、専用割り当てを意味しない。

Worker は水平スケール可能とし (Wasmtime Worker x N)、JetStream の共有 Pull Consumer により負荷分散する。

### 3.6 実行モデルと Coldstart 対策

「高速起動」を満たすため、Worker は以下を実装しなければならない (MUST)。

* **Component キャッシュ**: in-memory LRU + ローカルディスクの事前コンパイル済みモジュール (cwasm)。Object Storage からの取得は cache miss 時のみ。キャッシュキーは `wasm_sha256`。
* **事前コンパイル**: `Engine::precompile_component` で生成した cwasm を配布・キャッシュし、実行時の JIT コンパイルを回避する。ロードは `Component::deserialize`（自プラットフォームが生成した信頼できる cwasm のみに用いる。他テナントの cwasm を読み込んではならない (MUST NOT)）。
* **事前インスタンス化**: import 解決済みの `InstancePre` を Component ごとに保持し、リクエスト毎は `Store` 生成 + `instantiate` のみとしてホットパスを短縮する。
* **プーリングアロケータ**: `InstanceAllocationStrategy::Pooling` でインスタンス用メモリ / スロットを事前確保し再利用する（`InstancePre` とは別レイヤの最適化）。
* cwasm は Wasmtime / ターゲットのバージョンに依存するため、バージョン不一致時は元 wasm から再コンパイルにフォールバックする。

### 3.7 認証・認可・テナント分離

#### 認証

* クライアントはテナントスコープ付きトークン（API キーまたは JWT）を提示しなければならない (MUST)。トークンは発行・失効・有効期限・スコープを持つ（`api_tokens` 表）。トークン実体はハッシュ保存し、平文を保存してはならない (MUST NOT)。
* 最初のテナント / 管理者トークンの発行（ブートストラップ）は §9 に従う。

#### 認可（スコープと RBAC）

* トークンはスコープ集合を持つ: `read`（参照）/ `invoke`（実行）/ `deploy`（Component・Version 登録）/ `admin`（テナント管理・Capability 承認・削除）。
* ユーザー `role` とスコープの対応: `member` は最大 `read` / `invoke` / `deploy`、`admin` は加えて `admin` を取得しうる。API キーは発行時にこれ以下へ絞り込める（最小権限）。
* すべての API は「リソースの所有テナント = 呼び出しトークンのテナント」を検証しなければならない (MUST)。識別子は推測困難なランダム ID とし、ID 推測による越権（IDOR）を防ぐ。所有テナント外のリソースには `404` を返す。
* 各 API の最低要求スコープ:
  * 参照系 (`GET`) = `read`、`POST /invoke` = `invoke`、Component / Version 登録 = `deploy`。
  * **Capability の承認、Component / Version の削除 (`DELETE`)、テナント / ユーザー / トークン管理は `admin` を必須とする (MUST)。**
* テナント分離は全レイヤで強制する: PostgreSQL（`tenant_id` + RLS。§3.2）、Object Storage（プレフィックス / オブジェクト限定資格。§3.4）、NATS（テナント subject + ロール別権限 + ジョブ署名。§3.3）。

#### 監査

以下の高影響操作は監査ログに記録しなければならない (MUST): 認証失敗、認可拒否 (403)、Capability 承認、Component / Version 削除、トークン発行 / 失効、テナント / ユーザー作成。その他の認証・認可判断は記録すべきである (SHOULD)。

### 3.8 Observability

* **ログ**: Component の WASI stdout / stderr を Worker が `WasiCtx` の pipe で捕捉し、`tenant.{id}.component.logs` 経由で永続化する。大量ログは PostgreSQL ではなく Object Storage / ログ基盤へ保存し、保持期間（TTL）を定める。`execution_id` を correlation ID として全ログ・結果に付与する。
* **メトリクス**: Control Plane / Worker は per-component の実行数・レイテンシ (p50/p99)・エラー率・timeout 率・メモリ使用量を Prometheus 形式で公開する (SHOULD)。
* **ヘルスチェック**: Control Plane / Worker は `/healthz`（liveness）と `/readyz`（readiness）を提供しなければならない (MUST)。
* 分散トレーシングは将来拡張とする（§10）。

---

## 4. Component

### 4.1 定義

Component とは「WASI Component Model に準拠した実行可能単位」である。ユーザーがデプロイ可能な最小機能単位とする。

例

```text
markdown.wasm
resize.wasm
validator.wasm
```

### 4.2 インターフェース (WIT World)

Component は本プラットフォームが定義する標準 world に準拠しなければならない (MUST)。v1 の標準 world（例）:

```wit
package faas:component@1.0.0;

interface types {
  enum error-kind { invalid-input, runtime, resource-exceeded }
  record handler-error {
    kind: error-kind,
    message: string,
  }
}

world handler {
  use types.{handler-error};
  /// 入力バイト列を受け取り、結果バイト列または構造化エラーを返す
  export handle: func(input: list<u8>) -> result<list<u8>, handler-error>;
}
```

* `record` / `enum` 等の型定義は `interface` に置き、`world` からは `use` で取り込む（`world` 直下に型を散在させない）。
* Component が import できる WASI Preview 2 インターフェースは、付与された Capability（§4.4）に対応するもののみとする。world で表現できない / 未許可の import は拒否する。
* API の `input` / `output`（JSON）と WIT 値の対応（lift / lower 規約、JSON ↔ `list<u8>` のシリアライズ）を定める。大きなペイロードは Storage 参照とする（§6.4）。

### 4.3 実行特性と Resource Limit

実行特性

* **Stateless**: Component はリクエスト間で状態を保持してはならない (MUST NOT)。
* **冪等性**: 副作用（Storage 書き込み・外部 API 呼び出し等）を持つ Component は冪等でなければならない (MUST)。JetStream は at-least-once 配送のため再実行されうる（§6.6）。
* **決定性**: clocks / random / 外部 import を用いると非決定的になりうる。決定性が必要な場合はこれらを Capability で禁止する。

Resource Limit（Version ごとに `resource_limits` として保存。単位・既定値・上限・適用機構を明示）

| 項目 | 単位 | 既定値 | 上限 | 適用機構 |
|------|------|--------|------|----------|
| `max_memory` | バイト | 128 MiB | 1 GiB | Wasmtime `StoreLimits` |
| `max_wall_time` | ミリ秒（壁時計・ゲスト実行） | 1000 | 30000 | epoch interruption（別スレッド ticker） |
| `max_execution_time` | ミリ秒（壁時計・ホスト関数込み総時間） | 5000 | 60000 | tokio タイムアウト |
| `max_fuel`（任意・決定性用） | fuel 単位 | 無効 | — | Wasmtime fuel |

時間・暴走制御の整理（重要）

* **暴走中断の主機構は epoch interruption** とする。epoch は別スレッドの ticker でカウンタを進める方式であり、**壁時計（経過時間）ベース**である（CPU 会計機構ではない）。したがって `max_wall_time` はゲスト計算の経過時間上限を表す（初版で `max_cpu_time` と誤記していたが、epoch は CPU 時間を測れないため改名・訂正した）。
* `max_execution_time`（tokio タイムアウト）は WASI ホスト関数内のブロッキングを含む総経過時間の上限。epoch はゲスト内ループは中断できるがホスト関数中のブロッキングは止められないため、両者を併用する。
* fuel と epoch は暴走中断の**代替関係**にある。fuel は決定的な命令量カウントだが計装オーバーヘッドを伴うため、**既定では epoch のみを適用**し、決定性が要件の Component に限り `max_fuel` を有効化する（その場合も時間上限は併用してよい）。両機構を常時同時適用はしない。
* CPU 時間そのものを制限したい場合は OS レイヤ（cgroup cpu quota / Pod の CPU limit）で行う。
* 超過時は実行を強制終了し、時間超過は `timeout`、メモリ / fuel 超過は `failed` として記録する。
* テナント単位の同時実行クォータ・レート制限は §8 で定める。

### 4.4 Capability モデル

* 既定は **deny-all** とする (MUST)。`capabilities = {}` は何も許可しないことを意味する。明示的に付与された Capability のみ有効。
* 付与単位は Component Version とし、宣言を `component_versions.capabilities` に保存する。付与（承認）は `admin` スコープを要する (MUST。§3.7)。
* `capabilities` の JSON 形（例）:

```jsonc
{
  "net":   { "allow_outbound": ["api.example.com:443"] },  // 許可 outbound ホスト:ポート
  "fs":    { "scratch_dir": true },                        // 使い捨て一時ディレクトリのみ
  "env":   ["LOG_LEVEL"],                                   // 渡す環境変数の許可リスト
  "stdio": { "inherit": false }
}
```

* Worker は付与された Capability を `wasmtime-wasi` の linker 設定（preopened dirs、許可 outbound、env、stdio）へ機械的にマッピングする。`§6.2` の検証はこの許可リストと WIT import を照合する。
* 未許可のネットワーク / ファイルシステム / プロセス操作は許可してはならない (MUST NOT)。

---

## 5. 実行フロー

### 5.1 Deploy

```text
Client
 ↓ POST /components/{id}/versions （wasm 本体をアップロード、要 deploy スコープ）
Axum: 検証パイプライン（§6.2。サイズ上限→隔離検証→import照合）
 ↓ 検証通過した本体を保存
Object Storage（tenants/{tenant_id}/components/...）
 ↓ storage_uri / wasm_sha256 / capabilities / limits を記録（status=pending→active）
PostgreSQL
```

検証を通過するまで Version は `pending` とし、実行不可とする (MUST)。

### 5.2 Invoke（非同期）と大容量 I/O

```text
[大入力がある場合] Client → POST /uploads（§6.0）→ execution_id 予約 + 署名付き PUT URL + input_ref を取得
                   → upload_url へ本体を PUT
Client
 ↓ POST /invoke （要 invoke スコープ。大入力時は予約済み execution_id + トップレベル input_ref を指定）
 ↓ → execution_id を即時返却
Axum: executions に pending で INSERT（大入力時は予約済み execution_id を使用）、
      invoke メッセージ（input or input_ref + ジョブ署名トークン + reply-to）を
      tenant.{id}.component.invoke へ publish
 ↓
NATS JetStream（共有 Pull Consumer = competing consumers）
 ↓
Worker: Component を取得（cache miss 時のみ read-only）→ 入力取得（input_ref があれば read-only で取得）→ 実行
 ↓ 小さい結果はインライン、大きい結果は出力キー限定 write 資格で Storage に書き output_ref を作る
 ↓ 結果（or output_ref）・ログを tenant.{id}.component.result / .logs へ publish（署名トークンを echo）
NATS JetStream
 ↓
Axum: 署名・execution_id・テナント・状態遷移（CAS）を検証して永続化
 ↓
PostgreSQL
 ↓
Client: GET /executions/{id}（ポーリング）または SSE で結果取得
```

結果取得方式は非同期を基本とする（`execution_id` 返却 + ポーリング / SSE）。同期 reply（将来スコープ）は §6.3 を参照。

---

## 6. API

すべての API は認証ヘッダ `Authorization: Bearer <token>` を要する (MUST)。各 API の最低要求スコープは §3.7 に従う。エラーは共通形式（§6.5）で返す。識別子は推測困難なランダム ID とする。JSON 例中の `//` はコメント表示のための注釈であり、実際のリクエスト / レスポンス本体は純粋な JSON とする（コメントを含めてはならない）。

### 6.0 管理・認証 API

ブートストラップ手順は §9 を参照。

```http
POST   /admin/tenants               # テナント作成 + 初期管理者ユーザー（システム管理者のみ）
POST   /tenants/{id}/users          # ユーザー作成（admin スコープ）
POST   /tokens                      # トークン発行（スコープを指定。admin スコープ）
DELETE /tokens/{id}                 # トークン失効（admin スコープ）
POST   /auth/login                  # tenant_slug + email + password → 短命トークン交換
POST   /uploads                     # 大入力アップロード用の署名付き PUT URL 発行（invoke スコープ。§5.2 / §6.4）
```

**login（v1 はパスワード認証）**: `POST /auth/login` は `tenant_slug` + `email` + `password` を受け取る。email はグローバル一意ではない（`UNIQUE (tenant_id, email)`。§3.2）ため、`tenant_slug`（`tenants.slug`）でテナントを解決し `(tenant_id, email)` で一意にユーザーを特定しなければならない (MUST)。`users.password_hash`（argon2id 等で保存）と照合し、一致時は当該ユーザーの role 天井（§3.7）以下のスコープを持つ短命トークンを発行し `api_tokens` に記録する。パスワードは平文保存・ログ出力してはならない (MUST NOT)。将来的に OIDC 連携を追加してよい（§10）。

**login の反自動化制御** (MUST): `/auth/login` は認証前エンドポイントで §8 のテナント別レート制限が効かないため、IP / `(tenant_slug, email)` 単位のレート制限と、連続失敗時のバックオフ / 一時ロックアウトを適用する。これらのカウンタは Axum x N をまたぐため §8 の共有ストアで集計する (MUST)。ユーザー / テナントの存在有無を推測させないよう、失敗応答とエラー・応答時間を均一化する。失敗は監査ログ（§3.7）に記録し、失敗カウントを抑止トリガに結線する。

**トークン発行の権限検証** (MUST。権限昇格防止): `POST /tokens` は、要求スコープが「呼び出し元トークンのスコープ以下」**かつ**「対象 `user_id` の role 天井（§3.7）以下」の両方を満たすことを検証する。`user_id` を持たないサービストークンの天井は発行元 admin が明示指定する。

**大入力アップロード** (`POST /uploads`): 大入力（インライン上限超過。§6.4）を invoke 前に退避するための署名付き PUT URL を発行する。サーバは `execution_id` を**予約**（採番）してオブジェクトキー `tenants/{tenant_id}/io/{execution_id}/input` を確定し、当該キー限定・短 TTL（§3.4 の write 資格と同条件）の PUT URL を返す。`/uploads` は executions 行を INSERT しない（行は `component` / `version` が確定する `/invoke` 時点で INSERT する。§6.3）。予約済み `execution_id` は採番にのみ用い、`component_id` / `version_id` NOT NULL（§3.2）と矛盾しない。

Response `201 Created`

```jsonc
{
  "execution_id": "exec_xxx",
  "input_ref": "tenants/ten_xxx/io/exec_xxx/input",
  "upload_url": "https://...signed-PUT...",
  "expires_at": "..."
}
```

クライアントは `upload_url` へ本体を PUT し、続く `POST /invoke` で予約済み `execution_id` と `input_ref` を指定する（`/invoke` はその execution_id で行を INSERT する。新規採番はしない）。Axum は §3.4 の通り `input_ref` が当該 execution_id のキーに完全一致することを検証する (MUST)。

**孤児 execution / 孤立オブジェクトの回収** (MUST): `/uploads` で予約後に `/invoke` されない execution_id と、PUT 済みだが未使用の `io/{execution_id}/input` オブジェクトは、アップロード資格 TTL と同期した有効期限で失効させ、定期 GC（バケットライフサイクル / reaper）で物理削除する。予約のみで未 INSERT の execution は executions 行を持たず、in-flight カウンタの INCR も `/invoke` 時点で行われるため、`max_concurrent_executions`（§8、対象は `pending`+`running`）には計上されない。

### 6.1 Deploy Component（メタデータ作成）

```http
POST /components
Authorization: Bearer <token>          # deploy スコープ
Content-Type: application/json
```

Request（`name`: 必須・string・テナント内一意）

```jsonc
{
  "name": "markdown"
}
```

Response `201 Created`（`component_id`: string）

```jsonc
{
  "component_id": "cmp_xxx"
}
```

### 6.2 Upload Version（本体アップロードと検証）

```http
POST /components/{component_id}/versions
Authorization: Bearer <token>          # deploy スコープ
Content-Type: multipart/form-data       # または事前発行の署名付き URL へ直接 PUT
```

フィールド: `version`（必須・semver）、`wasm`（必須・バイナリ本体）、`capabilities`（任意・§4.4）、`resource_limits`（任意・§4.3）。

Control Plane は Storage 保存前に以下の検証を**この順序で**必須実行する (MUST)。失敗時は `422 Unprocessable Entity`。検証時 DoS（巨大 / 深くネストした wasm によるパーサのメモリ・CPU 枯渇）を防ぐため、検証は隔離して行う。

1. **アップロードバイト数上限をストリーミング中に強制**（上限超過は受信打ち切り）。
2. **隔離検証**: `wasm-tools` 等による Component Model 妥当性検証を、メモリ / 時間 / 再帰深さの上限付き・別プロセス（サンドボックス）で実行。同時検証数も制限する。
3. WIT import を承認済み Capability（§4.4）の許可リストと照合し、未知 / 未許可 import を拒否。
4. **Export contract 適合検証**: Component が宣言する WIT world が、プラットフォーム標準 world（§4.2 `faas:component`）に**契約として適合**することを確認する。具体的には (a) 必須 export（`handle: func(input: list<u8>) -> result<list<u8>, handler-error>` を含む `world handler` の全 export）が**シグネチャ込みで**揃っていること、(b) `handler-error` 等の型が標準定義と構造的に一致すること、(c) world のメジャーバージョンが受理可能であること（semver 互換。非互換メジャーは拒否、マイナー差は許容方針を定める）を、`wasm-tools` 等による型レベル照合で判定する。core module（Component でない wasm）・export 欠落・シグネチャ不一致・非互換 world は `422` で**人間可読なエラー**（不足/不一致の export 名とシグネチャを含む）を返す。import の照合（step 3）が「呼んでよい外部」を縛るのに対し、本 step は「プラットフォームが呼べる入口」を保証する。
5. サイズ・宣言メモリ上限の最終チェック、`wasm_sha256` 算出、署名 / 出所（テナント・ユーザー）の記録。
6. 検証通過まで `pending`（実行不可）。

Response `201 Created`

```jsonc
{
  "version_id": "ver_xxx",
  "version": "1.0.0",
  "status": "active"
}
```

### 6.3 Invoke Component

```http
POST /invoke
Authorization: Bearer <token>          # invoke スコープ
Content-Type: application/json
Idempotency-Key: <任意>                 # §6.6
```

Request（`component`: 必須、`version`: 任意=latest active、`input` / `input_ref`: 任意、`execution_id`: 任意）

```jsonc
{
  "component": "markdown",
  "version": "1.0.0",
  "input": {},
  "input_ref": null,
  "execution_id": null
}
```

`input`（インライン）と `input_ref`（Storage 退避。§5.2）は排他とする。インライン上限は §6.4 の閾値に従う。`input_ref` を使う場合は先に `POST /uploads`（§6.0）で予約した `execution_id` と `input_ref` を指定し、本 API はその予約済み `execution_id` で executions 行を INSERT する（`execution_id` 未指定時はサーバが新規採番して INSERT）。

Response `202 Accepted`（非同期。`execution_id`・`status`）

```jsonc
{
  "execution_id": "exec_xxx",
  "status": "pending"
}
```

同期 reply（**実装済み**。§14 / §15 M6）。`POST /invoke?wait=1` で次に従う: JetStream とは別に Core NATS の reply subject + correlation で待ち受け、JetStream は永続記録に専念する。reply 先は invoke メッセージの `reply-to` ヘッダで Worker へ伝搬する。Axum x N 構成では待受インスタンスを reply subject に識別子（CP instance id, `INSTANCE_ID`）として含め、どのインスタンスが reply を保持するかを一意にしなければならない。上限レイテンシ（`SYNC_REPLY_TIMEOUT_MS`）内に reply が届かなければ 202 + execution_id へ縮退する。

### 6.4 Execution Status

```http
GET /executions/{id}
Authorization: Bearer <token>          # read スコープ
```

Response `200 OK`

```jsonc
{
  "execution_id": "exec_xxx",
  "component": "markdown",
  "version": "1.0.0",
  "status": "succeeded",
  "output": {},
  "output_ref": null,
  "error": null,
  "created_at": "...",
  "started_at": "...",
  "finished_at": "..."
}
```

* `status`: `pending | running | succeeded | failed | timeout`。
* インライン上限は **256 KiB** とする（NATS メッセージ既定上限 1 MiB に対し余裕を確保）。これを超える `input` / `output` は Object Storage に退避し `input_ref` / `output_ref`（URI）で参照する。
* 所有テナント外の `id` には `404 Not Found` を返す。

### 6.5 Execution Logs / 共通エラー形式

```http
GET /executions/{id}/logs
Authorization: Bearer <token>          # read スコープ
```

ログはシークレットをマスキングし、保持期間 (TTL) を定める。

共通エラー形式（実体は純 JSON）

```jsonc
{
  "error": {
    "code": "not_found",   // unauthorized | forbidden | not_found | invalid_input | validation_failed | conflict | rate_limited | internal
    "message": "human readable",
    "retryable": false
  }
}
```

主なステータス: `400`（入力不正 = `invalid_input`）、`401`（未認証）、`403`（権限不足 / スコープ不足）、`404`（不存在 / 越権の秘匿）、`409`（冪等競合 = `conflict`）、`422`（検証失敗 = `validation_failed`。§6.2）、`429`（レート / クォータ超過 = `rate_limited`）、`5xx`（内部）。

### 6.6 リトライ・冪等性・タイムアウト

冪等の三層を次のように結線する。

* **Idempotency-Key（クライアント提示）**: `executions.idempotency_key` に保存し `UNIQUE (tenant_id, idempotency_key)` で一意化（テナント名前空間）。同一キーの再送は既存の `execution_id` を返す。本文が前回と異なる場合は `409 conflict`。キーは最大長・文字種・TTL を定める。
* **execution_id（サーバ生成）**: 実行状態の最終的な冪等キー。`pending → running` を条件付き UPDATE（CAS）で保護する。
* **Nats-Msg-Id**: invoke メッセージに `execution_id` を `Nats-Msg-Id` として付与し、JetStream の重複排除で配送段の重複を抑止する。

その他

* JetStream Consumer に `max_deliver` / `ack_wait` / backoff / DLQ subject（`component.failed`）を設定する。結果出所トークンの有効期限は最悪滞留時間（固定 `ack_wait` なら `ack_wait × max_deliver`、backoff 構成なら `Σ backoff[i]`）+ 実行上限 + 余裕 以上とする（§3.3。再配送で結果が失われないため）。
* エラーを **リトライ不可**（ユーザーコードの trap・入力不正・リソース超過）と **リトライ可**（インフラ起因）に分類し、前者は即 `failed` とする。
* タイムアウトは epoch interruption / tokio タイムアウトで中断し `timeout` として記録する。
* **異常終端の確定とカウンタ減算** (MUST): `.result` を受信しない経路（`max_deliver` 枯渇で DLQ = `tenant.{id}.component.failed` に落ちたジョブ、Worker クラッシュ等で `.result` を一度も publish しないジョブ）も、必ず終端状態へ確定し in-flight カウンタ（§8）を減算しなければならない。具体的には、(a) Control Plane は `.failed` / DLQ を購読（§3.3）し、該当 execution を `failed` へ CAS 遷移してカウンタを減算する。(b) `.result` も `.failed` も来ない無音失踪に備え、`pending` / `running` のまま「最悪滞留時間（上記）+ 実行上限 + 余裕」を超過した行を定期 sweep（reaper）で `timeout` / `failed` へ確定しカウンタを減算する。この結線が無いとカウンタがリークし、当該テナントの `max_concurrent_executions` を恒久消費して新規 invoke が永続 429 になる（テナント自己 DoS）。保険として in-flight カウンタに TTL を付してよい (SHOULD)。

### 6.7 Component / Version 管理

```http
GET    /components                        # 一覧（read スコープ）
GET    /components/{id}/versions          # バージョン一覧（read）
DELETE /components/{id}                    # soft delete（admin。参照中の実行があれば保護）
DELETE /components/{id}/versions/{ver}     # soft delete（admin）
PUT    /components/{id}/active-version     # 現行版の切替（admin。ロールバック用）
```

version は semver で不変とし、同一 version の上書きを禁止する (MUST NOT)。`latest` は `components.active_version_id`（既定は最新の `active`）で解決する。ロールバックは `active-version` を過去の version に切り替えることで行う。削除は soft delete とし実行履歴の整合を保つ。

---

## 7. セキュリティ

実行環境は多層防御によりサンドボックス化する。

Wasmtime はメモリ安全性と Capability ベースのアクセス制御を提供するが、以下までは保証しない: サイドチャネル / Spectre、リソース公平性、同一プロセス内マルチテナント時のプロセス / カーネル分離、JIT 自体の脆弱性。これらは別レイヤで対処しなければならない (MUST)。

禁止事項（Capability で明示許可されない限り）

* ホストファイルシステムへの直接アクセス
* 任意プロセス生成
* 未許可ネットワークアクセス

多層防御

* Capability は既定 deny-all、明示付与のみ（§4.4）。承認は `admin` スコープ必須（§3.7）。
* 異テナント / 高リスク Component は別 Worker プロセスまたは別 Pod / Node で実行する (SHOULD)。
* OS レイヤで seccomp-bpf、NetworkPolicy、非 root 実行、read-only root filesystem を併用する。
* リソース枯渇 / DoS は §4.3 の制限と §8 のテナント別クォータ・レート制限・バックプレッシャ、§6.2 の検証時 DoS 対策で防ぐ。
* 結果 / ログにシークレットを残さない。保存時暗号化・保持期間・マスキングを適用する（§3.8）。将来の Secrets Manager（§10）と連携する。
* NATS / Object Storage への接続は認証・最小権限・テナント分離・結果出所認証を強制する（§3.3 / §3.4）。
* 高影響操作は監査ログに記録する（§3.7）。

---

## 8. スケーリングとクォータ

Control Plane（Axum x N）

```text
Axum x N   ── Ingress / Load Balancer で水平分散（ステートレス）
```

Worker（Wasmtime Worker x N）

```text
Wasmtime Worker x N   ── JetStream 共有 Pull Consumer（competing consumers）で負荷分散
```

* Axum の分散は Ingress / LB が担う。Worker の分散は JetStream の competing consumers が担う（「NATS が負荷分散」を両者一括りにしない）。

テナント別クォータ / レート制限（`resource_limits` と別に、テナント単位で保存。グローバル既定をテナントごとに上書き可）

| 項目 | 単位 | 既定値 | 上限 | 評価窓 / 適用箇所 |
|------|------|--------|------|------------------|
| `max_concurrent_executions` | in-flight 数（pending+running）/ テナント | 20 | 200 | 原子的カウンタ（共有ストア） |
| `invoke_rate` | req / 秒 / テナント | 50 | 500 | スライディングウィンドウ（共有ストア） |
| `MaxAckPending` | 未 ack メッセージ数 / **lane（= テナント）** | `max_concurrent_executions` + 8 | 208 | JetStream Consumer 設定（Control Plane が導出。overflow lane のみ固定 1000） |

* **分散カウンタの共有状態** (MUST): Axum はステートレス x N であるため、レート制限・同時実行カウント・login 失敗カウント（§6.0）をインスタンスローカルで保持すると上限が実効 N 倍に緩み正しく効かない。これらは**全 Axum インスタンスで共有する低レイテンシストア**（Redis 等のトークンバケット / カウンタ、または PostgreSQL のアトミックカウンタ）で集計しなければならない。
* **同時実行 admission の判定式** (MUST): `max_concurrent_executions` の対象は **in-flight 数（`pending` + `running`）** とする。`/invoke` 受付時に共有ストアの原子操作（atomic INCR）で当該テナントの in-flight を予約・判定し、上限到達なら INSERT せず `429` を返す。これにより、受付は `pending` だが計上は `running` 化後、という時間差で大量の `pending` をバースト素通しさせる穴と、N インスタンスが同じ低カウントを読む TOCTOU を防ぐ。カウンタは終端状態（`succeeded` / `failed` / `timeout`）到達時に減算する。**正常な `.result` 経路だけでなく、DLQ / 失敗 / 無音失踪の異常終端でも必ず終端化して減算しなければならない (MUST。§6.6)**。さもないとカウンタがリークし当該テナントの上限を恒久消費する。
* **カウンタとDBの整合（ドリフト補正）** (MUST): DB の `pending` + `running` 行数（§3.2）をカウンタの**真実**とする。定期 reaper がテナント別に `COUNT(*) WHERE status IN ('pending','running')` を集計して共有ストアのカウンタへ原子的に再同期し（再起動時の復元・ドリフト是正）、あわせて §6.6 の sweep で滞留行を終端化する。再同期中の一時的な過小評価は許容する。
* **共有ストア障害時の挙動（fail-open / fail-closed）** (MUST): カウンタは性能制御であると同時にセキュリティ制御でもあるため、対象別に方針を定める。login 失敗ロックアウト等のセキュリティ制御は**既定 fail-closed**（共有ストア到達不能時は拒否）。`invoke_rate` / 同時実行は可用性優先で **fail-open を選んでよいが、縮退をログ・メトリクス・監査（§3.7）に記録しアラートを上げる**、または PostgreSQL アトミックカウンタへ縮退する。共有ストア自体も単一障害点にしないため HA 構成または DB 縮退パスを備える（§9。NATS の R≥3 / DB の FORCE RLS と同等の可用性方針）。
* 永続先（上限の設定値）は `tenants.quotas` JSONB（§3.2。上書き分のみ格納）とし、グローバル既定 → テナント上書きの優先順位で解決する（カウンタの実体である共有ストアとは別。設定値は DB、実行時カウンタは共有ストア）。
* クォータ / レート超過時は `429`（`rate_limited`）で拒否し、`Retry-After` を付す。キュー滞留時もバックプレッシャとして `429` を返す。
* **公平性の責務分界（M8 で 3 層に確定）**: テナント間の公平性は次の 3 層で担う。(1) **受付層** = Axum の admission control（in-flight = `pending`+`running` と `invoke_rate`。テナント自身のクォータを守る）。(2) **配送層** = lane Consumer の per-tenant `MaxAckPending`（§3.3。1 テナントの未 ack が他テナントの配送枠を奪わない）。(3) **実行層** = Worker の per-lane 実行クレジット（1 プロセスが 1 テナントのジョブで実行スロットを占有しない）。`MaxAckPending` は lane ごとに `max_concurrent_executions` から導出されるため、**全テナント合算の目安式は不要になった**（M8 以前は単一の共有 Consumer に合算上限 1000 を置いていたため、`Σ(アクティブテナント数 × max_concurrent_executions) ≲ MaxAckPending` を運用目安とせざるを得ず、これを超えると『1 件しか投げていないテナントが他テナントの負荷で配送されない』というクォータ劣化が起きた）。運用条件は `W × WORKER_LANE_CONCURRENCY ≳ max_concurrent_executions`（W = Worker 台数）と `WORKER_MAX_CONCURRENCY ≳ アクティブ lane 数` の 2 本。重み付き（優先度）分離は引き続き将来導入とする（§10）。

---

## 9. 初期セットアップ / プロビジョニング

* **DB マイグレーション**: スキーマ（§3.2）をマイグレーションツールで適用し、全テナント表に `ENABLE / FORCE ROW LEVEL SECURITY` と RLS ポリシーを設定する。アプリ接続ロールは `BYPASSRLS` を持たない非特権ロールとする。
* **NATS プロビジョニング**: テナント横断のストリーム（クラスタ R ≥ 3、**WorkQueue retention**）を作成する。**Control Plane が stream の唯一の作成者**であり、`component.invoke` の lane Consumer も Control Plane が自動で用意するため**手動プロビジョニングは不要**（M8, §3.3 / §8）。あわせて Control Plane が `tenant.*.component.result` / `.logs` と **`.failed` / DLQ** を購読する Consumer を作成する（`.failed` 購読は異常終端の確定とカウンタ減算のため。§6.6 / §3.3）。subject permission を Control Plane / Worker のロール別に設定する（§3.3）。テナント追加時の Consumer 再構成は Control Plane の reconcile が自動で行う（運用手順は不要）。
* **Object Storage**: バケットと、テナントプレフィックスを跨がせないバケットポリシーを作成する（§3.4）。
* **Shared Store（Redis 等）プロビジョニング**: レート制限 / 同時実行 in-flight / login 失敗カウンタ（§8）の共有カウンタ用に Redis 等を用意する。SPOF を避けるため HA 構成（または §8 の DB 縮退パス）とし、障害時の fail-open / fail-closed 方針（§8）に従う。接続資格を Axum へ供給する。
* **ブートストラップ（最初のテナント / 管理者）**: システム管理者が `POST /admin/tenants`（システム管理権限で保護。CLI / 初期シークレット等の帯域外手段で認可）でテナントと初期管理者ユーザー（初期パスワードを設定）を作成し、初回 `admin` トークンを発行する。以降のテナント内ユーザー / トークンは `admin` トークンで作成し、ユーザーは `POST /auth/login`（§6.0）でトークンを取得する。これにより「最初のトークンをどう作るか」の鶏と卵を解消する。
* **署名鍵のプロビジョニング**: 結果出所認証（§3.3）の Ed25519 鍵ペアを KMS / Secrets で生成し、Control Plane へ秘密鍵、Axum へ公開鍵（`kid` 付き）を供給する。Worker には鍵を供給しない。ローテーション時は overlap 期間中、新旧公開鍵を Axum が併存保持する。**overlap 期間 ≥ 旧 `kid` で発行されうるトークンの最大有効期限**（= §3.3 の TTL 式）を満たさなければならない (MUST)。旧公開鍵の撤去は、旧 `kid` で署名された in-flight トークンがすべて失効した後に行う（早期撤去すると後着の正規結果が署名検証失敗で破棄され実行喪失するため）。
* **Worker / Axum 設定**: NATS 接続資格・subject permission、DB 接続（非特権ロール）、Storage エンドポイント、署名検証用公開鍵（Axum）、Shared Store 接続資格（Axum）等を環境変数 / シークレットで供給する。

---

## 10. 将来拡張 / スコープ外（概要）

詳細なステータスは §14 のスコープ表で管理する。将来拡張の対象（Cron Job / 外部イベントトリガーは §15 M6 で実装済み。§14 / §11 参照）:

* Workflow Engine
* Secrets Manager
* Distributed Tracing（OpenTelemetry。WASM + NATS は経路がブラックボックス化しやすいため早期導入が望ましい。§3.8）
* AI Service Integration（§13）
* Multi Region Deployment
* 結果取り込み専用サービス（Result Ingestor）の分離 — 現仕様は `.result` を Axum が購読・検証・永続化する（§2 / §5.2）。Axum はステートレス x N でスケールするが、高頻度ジョブ・大量ログ環境では「API(read) と 結果ingest(write)」を別サービスに分離して書込経路を独立スケールさせてよい。`.result` の署名検証・冪等性ロジック（§3.3 / §6.6）は分離先にそのまま移せる。
* テナント別 Consumer / 優先度分離（共有 Pull Consumer の合算頭打ち対策。§8）

LLM は外部サービスとして統合可能とする。

```text
Component
 ↓
AI Service（外部）
```

---

## 11. イベントソースとトリガー

「イベント駆動」を満たすため、HTTP `POST /invoke` 以外の起動経路を定義する。v1 のスコープは §14 を参照。

* トリガー登録モデル: どのイベント源（Object Storage イベント、別 Component の出力、外部メッセージ等）がどの Component を起動するかを登録する。
* トリガー設定 API、イベントペイロード → Component `input` のマッピング、Component 間チェーンを規定する。

---

## 12. 設計原則

1. Control Plane と Data Plane を分離する
2. Component は Stateless を基本とし、副作用を持つ場合は冪等とする
3. イベント駆動を基本とする
4. Wasm を唯一の実行形式とする
5. 実行環境は多層防御でサンドボックス化する（「完全」と断言しない）
6. システム状態は PostgreSQL に集約する
7. Component は独立してデプロイ可能とする
8. マルチテナント分離を全レイヤ（DB / Storage / NATS）で強制し、結果の出所も認証する

---

## 13. 外部サービス統合（将来）

LLM 推論を含む AI 機能は本システムの実行モデル外とし、Component から外部 AI サービスを呼び出す形で統合する。呼び出しは Capability（許可 outbound ホスト。§4.4）で制御する。

---

## 14. スコープ表

| 項目 | v1 | 将来 | 備考 |
|------|----|----|------|
| HTTP Invoke（非同期） | ✅ | | §5.2 / §6.3 |
| 同期 Invoke（reply subject） | ✅ | | §6.3 / §15 M6 |
| マルチテナント認証・認可（RBAC / スコープ） | ✅ | | §3.7 / §6.0 |
| 管理・認証 API / ブートストラップ（login=パスワード認証） | ✅ | | §6.0 / §9 |
| 大入力アップロード API（POST /uploads） | ✅ | | §6.0 / §5.2 |
| OIDC 連携ログイン | ⬜ | ✅ | §6.0 / §10 |
| Capability モデル | ✅ | | §4.4 |
| 結果出所認証（ジョブ署名トークン） | ✅ | | §3.3 |
| Component キャッシュ / 事前コンパイル | ✅ | | §3.6 |
| 大容量 I/O 退避（input_ref / output_ref） | ✅ | | §3.4 / §5.2 / §6.4 |
| クォータ / レート制限 | ✅ | | §8 |
| 課金・メータリング（per-execution 計量 + 期間集計 + 利用量参照 API） | ✅ | | §15 M5 |
| 監査ログ | ✅ | | §3.2 / §3.7 |
| 外部イベントトリガー | ✅ | | §11 / §15 M6 |
| Cron Job | ✅ | | §11 / §15 M6 |
| Workflow Engine | ⬜ | ✅ | §10 |
| Secrets Manager | ✅ | ✅ | §10（M7c で実装済み: 封筒暗号 + write-only API + execution 基準の世代固定注入 + wrap-only の KEK ローテーション） |
| Distributed Tracing | ⬜ | ✅ | §3.8 / §10 |
| 共有ストア（レート制限カウンタ） | ✅ | | §8 |
| Result Ingestor 分離 | ⬜ | ✅ | §10 |
| AI Service 統合 | ⬜ | ✅ | §13 |
| Multi Region | ⬜ | ✅ | §10 |

LLM 推論機能は本仕様の対象外とし、将来的な外部サービス統合として扱う。

---

## 15. 実装ロードマップ

§14 のスコープ表は「v1 / 将来」の二値だが、v1 内も一度に作らず、**各マイルストーンが単体で動作・テスト可能**になるよう段階実装する。各段は前段の上に積む（前段を壊さない）。下表の M1〜M4 は §14 の v1✅ 範囲。M5（課金・メータリング基盤）は当初「将来⬜」だったが商用化の前提として先行実装済み（§14 のメータリング行は ✅）。M6（Invoke 経路の拡充: 同期 Invoke + 外部イベント/Cron トリガー）も当初「将来⬜」だったが商用化の前提として実装済み（§14 の同期 Invoke / 外部イベントトリガー / Cron Job 行は ✅）。M7（デプロイ運用とコンフィグ: canary / rollback / per-function env / Secrets Manager）も実装済み（§14 の Secrets Manager 行は ✅）。M8 以降が将来⬜。

### M1: 最小往復（Walking Skeleton）
**目標**: 1 つの Component が invoke され結果が返る経路を貫通させる。
- Axum: `POST /components`・`POST /invoke`・`GET /executions/{id}`（認証は固定トークンの仮実装で可）
- PostgreSQL: `components` / `component_versions` / `executions`（RLS は後段、まず単一テナント固定）
- NATS JetStream: `invoke` ストリーム + 共有 Pull Consumer、`result` 購読・永続化
- Worker: 汎用ランタイム（Wasmtime）、固定パスから wasm ロード、標準 WIT world（§4.2）で実行
- リソース制限: `max_memory`（StoreLimits）+ `max_wall_time`（epoch）の最小2つ
- **完了条件**: インライン入出力で end-to-end が動き、`status` が pending→running→succeeded に遷移する。

### M2: Component ライフサイクルとストレージ
**目標**: ユーザーが wasm をアップロードして動かせる。
- `POST /components/{id}/versions`（検証パイプライン §6.2: サイズ上限→隔離検証→import 照合）
- Object Storage（MinIO ローカル）: 本体保存、Worker は署名付き URL で read-only 取得（§3.4）
- §3.6 Component キャッシュ（`wasm_sha256` キー）+ 事前コンパイル
- バージョン管理（§6.7: 一覧・active-version 切替・soft delete）
- **完了条件**: アップロード→検証→デプロイ→invoke が動き、cache hit で coldstart が短縮される。

### M3: マルチテナントとセキュリティ強化
**目標**: 本番の信頼境界を全レイヤで成立させる。
- 認証/認可: §6.0 管理 API、`/auth/login`（パスワード）、RBAC/スコープ（§3.7）、§9 ブートストラップ
- テナント分離: 全表 `tenant_id` + RLS（`SET LOCAL`。§3.2）、Object Storage プレフィックス分離、NATS テナント subject + ロール別権限（§3.3）
- 結果出所認証: Ed25519 ジョブ署名トークン + TTL 整合（§3.3）
- 冪等性三層（§6.6）、監査ログ（§3.2 / §3.7）、Capability モデル（§4.4）
- 共有ストア（Redis）: レート制限 / 同時実行 / login 失敗カウンタ（§8）
- 大容量 I/O: `POST /uploads` + `input_ref` / `output_ref`（§3.4 / §5.2 / §6.4）
- **完了条件**: 複数テナントで分離が破れず、偽結果注入・IDOR・越権・ブルートフォースが各対策で塞がれていることをテストで確認。

### M4: 運用成熟度
**目標**: 運用に耐える可観測性と頑健性。
- Observability: メトリクス（Prometheus）、`/healthz`・`/readyz`（§3.8）、構造化ログ + correlation ID
- リトライ/DLQ の完全実装、孤児 execution GC（§6.0）、全リソース制限（fuel・タイムアウト）
- 残りのクォータ・バックプレッシャ（§8）
- **完了条件**: 障害注入（Worker 落下・再配送・タイムアウト）で実行喪失・二重実行が起きない。

### M5 以降（M5・M6・M7 実装済み / M8 以降が将来⬜）: 商用マルチテナント SaaS 化

M1〜M4 で「動く・安全・壊れない」基盤は成立した。M5 以降は **不特定多数のテナントが同居する商用クラウド SaaS** として成立させるための段階実装とする（M5 課金・メータリング、M6 Invoke 経路の拡充、M7 デプロイ運用とコンフィグまでは実装済み）。各段は M1〜M4 同様、**単体で動作・テスト可能**・前段を壊さない・**完了条件**を持つこと。

> **スコープ方針の変更**: 旧ロードマップは「AI 統合」を一マイルストーンに据えていたが、AI/LLM はプラットフォーム機能ではなく、テナントの Component が Capability（許可 outbound ホスト。§4.4 / §13）経由で外部サービスを呼ぶ既存枠で充足する。よって AI 統合はロードマップ項目から外す（プラットフォーム実装は不要）。Workflow Engine・Multi Region は商用 MVP の必須要件ではないため、需要が顕在化した時点で着手する**需要発火型**とし、固定順序に置かない。

#### M5: 課金・メータリング基盤
**目標**: テナント別の利用量を正確・冪等に計測し、課金可能にする（商用の前提条件）。
- per-execution の計測: CPU 時間（fuel/epoch から導出）・wall time・peak memory・I/O / egress バイト・呼び出し回数を `executions` に記録
- 冪等な計上: 再配送・worker 落下時に二重計上/欠落しないよう、結果出所認証（§3.3）と冪等性三層（§6.6）に結合した確定処理で集計
- テナント別の期間集計テーブル + 利用量参照 API（§6.0 配下、`read` スコープ）、クォータ（§8）超過の課金的扱い
- 集計セマンティクス（課金解釈の明示）: `invocation_count` と各終端 count は **全終端実行**で計上する（DLQ・timeout・sweeper 経路を含む）。リソース指標（CPU・wall・出力は SUM、peak memory は MAX）は **計測値を持つ succeeded 実行のみ**寄与する（その他は count を立てつつ 0 加算の「半端行」）。`SUM(cpu)` 等を「全終端の総コスト」と解さない。権威 limit を解決できない result は信頼境界外として invocation のみ計上しリソース指標は記録しない（fail-closed）。
- **完了条件**: 障害注入（再配送・worker 落下・タイムアウト）下でも計量が二重計上/欠落せず、テナント別に時間窓集計が一致する。worker 落下を sweeper が終端化した実行も invocation 集計に乗る（欠落しない）。

#### M6: 開発者向け Invoke 経路の拡充（実装済み）
**目標**: 非同期 invoke 一択から、商用で要求される起動形態を揃える。
- 同期 Invoke（§6.3）: Core NATS reply subject + correlation、Axum x N での reply 待受インスタンス識別（ステートレス前提との整合を設計で解く）。`POST /invoke?wait=1` が上限レイテンシ（`SYNC_REPLY_TIMEOUT_MS`）内に 200 + output を返し、reply subject に CP instance id（`INSTANCE_ID`）を埋めて当該インスタンスだけが reply を購読する。job_token 署名を verify してから返す（§3.3）
- 外部イベントトリガー（§11）: トリガー登録モデル（`triggers` / `cron_jobs`）、Object Storage イベント・Component チェーン・スケジュール（Cron）起動、イベントペイロード → `input` マッピング（`input_mapping`）。同一イベント / 同一 slot は配送台帳（`trigger_deliveries`）の PK + 冪等キーで 1 度だけ起動し、テナントは object キー / principal から導出して anti-spoof する
- **完了条件（達成済み）**: HTTP 同期呼び出しが上限レイテンシ内で結果を返し、Cron 登録で定時起動し、トリガー経路でも冪等性・テナント分離・計量（M5）が non-HTTP 起点で破れない。`crates/control-plane/tests/chaos_m6.rs` の S1（同期 invoke）/ S2（Cron）/ S3（トリガー）で end-to-end 検証。

#### M7: デプロイ運用とコンフィグ（実装済み）
**目標**: 本番運用に耐えるリリース安全性を与える。
- バージョン traffic splitting / canary（active-version の段階移行）、即時 rollback: 配分は `components` への additive 列（migration 0009。**新規テーブルを作らない**ことで GRANT / RLS ポリシー漏れという最大の退行リスクを構造的に消す）。版の選択は**乱数を使わず**ルーティングキーの SHA-256 からバケットを決定的に導出し、規則は `bucket < canary_weight` の単一不等式のみ（分配比率の正しさを DB 非依存の全列挙テストで証明でき、同一 Idempotency-Key の再送・cron の同一 slot 再発火・chain の再配送が常に同じ版へ落ちる）。解決点は `enqueue::resolve_version_for_enqueue` の 1 箇所で、HTTP / Cron / event / chain の全入口が通る（**どの入口も canary をバイパスできない**）。stable ポインタを動かす 3 操作は単一 UPDATE で完結し、必ず canary をクリアし、no-op 再 activate では `previous` を CASE ガードで保全する
- per-function の環境変数・設定: `function_configs`（migration 0010、行=1キー）。**注入面そのものを admin 承認の対象にする** —— `capabilities` を `{"imports":[...],"env":[...]}` の 2 キー構造へ拡張し、`env`（注入を許可する名前）は admin 専用エンドポイントでしか書けない。`POST /versions`（Deploy スコープ）から書けると、deploy トークンが `env:["PROD_API_KEY"]` を宣言した版を上げて `wasi:cli/environment` で読んだ値を invoke 出力へ返すだけで admin 専用の secret を取得でき、権限昇格になる（§4.4 の「付与は admin スコープを要する」MUST）
- Secrets Manager（§10）: XChaCha20-Poly1305 の封筒暗号（KEK → 行ごとの DEK → 値）で保存（migration 0011）。AAD に `(tenant_id, component_id, name)` と `(tenant_id, secret_id, version, kek_kid)` を束縛して**行の貼り替え**と **version の巻き戻し**を遮断する。版台帳は**追記専用**（`faas_app` に UPDATE/DELETE を GRANT しない）で、値の変更も KEK 再ラップも「新しい version 行の INSERT」で表現する。API は **write-only**（値を返す経路をコードに作らない）。注入は worker が CP の**内部専用エンドポイント**から専用トークン（`aud="job-env"`・job_token とは別ドメインタグ）で引き換える —— worker は KEK を持たない（keyless by design, §3.3）ため、平文は「CP のメモリ → HTTP レスポンス → worker のメモリ → `WasiCtx`」だけを通り NATS にも DB にも S3 にも永続化されない。**世代は execution に固定**する（`current_version` を参照すると、再配送の間に rotate が走ったとき同一 execution の 1 回目と 2 回目で別の資格情報が外部 API へ到達しうる）。引き換え失敗はすべて fail-closed。KEK ローテーションは wrap-only の再ラップで、**侵害復旧ではない**（侵害時の唯一の復旧経路は値そのものの rotate）。Capability（§4.4）の outbound 認証情報は「secret として保管し承認された env 名で注入する」ところまでで、実際に外へ出る経路（egress allowlist の実効強制）は M9
- **完了条件（達成済み）**: active-version を 10%→100% へ段階移行でき、ワンクリック rollback が効き、secret はログ・監査・他テナントへ漏れない（露出ガード §7 / §15）。`crates/control-plane/tests/chaos_m7.rs` の S1（canary 段階移行 + ワンクリック rollback）/ S2（env・secret 注入）/ S3（secret 非漏洩）で end-to-end 検証。実測で 100 リクエスト × weight 10/50/100 に対し canary 8/45/100 件（決定的期待値と完全一致）。

#### M8: 弾力スケールとテナント間アイソレーション
**目標**: 不特定多数テナントの同居で公平性と弾力性を成立させる。
- Worker オートスケール: JetStream の pending depth ベースで水平増減、scale-to-zero / from-zero coldstart（§3.6 キャッシュと結合）
- テナント別 Consumer（§8 / §10。共有 Pull Consumer の合算頭打ち・noisy-neighbor 対策）。**重み付き（優先度）分離は M9 以降**へ送る（M8 は等分のみ）
- Result Ingestor 分離（§2 / §10:776。**需要発火型**: `.result` の write 経路が頭打ちになった時点で分離。署名検証・冪等性ロジックはそのまま移設）
- **完了条件（M8 で実測可能な形へ改訂）**: (1) JetStream の未消化仕事量（全 lane の `num_pending + num_ack_pending` の合計）に応じ、リポジトリ内の参照アクチュエータが worker プロセス数を `SCALE_MIN_WORKERS` 〜 `SCALE_MAX_WORKERS` の範囲で自動増減し、scale-to-zero / from-zero が成立する。(2) 1 テナントのバースト負荷の最中に、他テナントが受けた 429 が **0 件**であり、他テナントの全ジョブが `succeeded` で終端し、その**キュー待ち時間**（`started_at - created_at`、いずれもサーバ時計）の最大値が規定の絶対上限を超えない。かつ「バーストが実際に滞留を起こしていた」ことを**負の対照**として同時に確認する。(3) 上記のスケール 1 サイクルを通して、実行回数が投入件数と一致し（再実行ゼロ）、DLQ 終端が 0 件である。`crates/control-plane/tests/chaos_m8.rs` の U1〜U4 で end-to-end 検証。
  > **改訂の理由**: 原文は「他テナントの **p99 レイテンシ**を劣化させない」だったが、これは本リポジトリでは測定できない —— (a) `docker-compose.yml` に Prometheus が無く、(b) `faas_execution_duration_seconds` は tenant ラベルを持たず observe 配線も未了（テナント別ヒストグラムは §3.8 のカーディナリティ方針と衝突する）、(c) 単一マシン上の分位点比較はフレークし、しかも「アイソレーションが壊れた」という誤った症状に見える。M6 / M7 が確立した「chaos は分布や時間依存の assert を持たない」作法とも矛盾する。そこで **サーバ時計で測るキュー待ちの絶対上限・429 の件数・順序の個数・live プロセス数の整数到達**へ翻訳した。テナント別の p99 そのものは、運用者が `executions` 生表へ `percentile_cont` を引く手順を README に置く（API として露出させると他テナントの統計が漏れるため露出しない）。

#### M9: サンドボックス強化・サプライチェーン
**目標**: 未信頼 wasm をマルチテナントで常時実行するための多層防御を完成させる（§12 原則 5）。
- プロセスレベル分離（§6.2 が「M4 以降の TODO」とした項目）: 検証・実行を別プロセス/別サンドボックスへ
- 署名付き Component と供給網検証（アップロード経路 §6.2 への署名検証追加）
- egress allowlist（§4.4 Capability）の実効的なネットワーク強制
- **完了条件**: 脅威モデルに基づくレッドチームテストで、悪意ある wasm が隣接テナント / ホスト / 未許可 outbound に到達できないことを確認。

#### M10: 可観測性の完成 / Multi Region（需要発火型）
**目標**: 商用 SLA とグローバル展開に備える。
- 分散トレーシング（OpenTelemetry。§3.8 / §10）: M4a の correlation ID を OTel スパンへ。**WASM + NATS 経路はブラックボックス化しやすく高レバレッジのため、M6 以降で前倒し着手してよい**
- Multi Region（§10。**需要発火型**: 地理分散要求が顕在化した時点で）
- **完了条件**: invoke → worker → result の経路がトレースで end-to-end に追え、リージョン障害時にテナントデータの一貫性が保たれる。

> **着手順の原則**: M5（課金）→ M6（Invoke 拡充）→ M7（デプロイ運用）→ M8（スケール/分離）→ M9（サンドボックス強化）を商用 MVP の基本線とする（M5〜M7 は実装済み）。分散トレーシング（M10）は運用デバッグ価値が高く前倒し推奨。Workflow Engine・Result Ingestor 分離・Multi Region は固定順序を持たない**需要発火型**で、対応する負荷・要求が顕在化した時点で割り込ませる。

> 原則: **M1〜M2 は単一テナント前提で薄く貫通させ、M3 で分離・セキュリティを一括導入**する。セキュリティを後付けと誤解しないよう、M3 のテナント分離は「機能追加」ではなく「全レイヤ改修」として計画する（tenant_id・RLS・subject 設計は M1 のスキーマ時点から列だけ用意しておく）。
>
> **露出ガード** (MUST NOT): M1〜M2 の固定トークン・RLS 無効・単一テナント構成は、信頼境界外（共有ネットワーク / インターネット到達可能なステージング・本番相当環境）へ展開してはならない。認証・RLS・テナント分離（M3）が成立するまで、本番相当の露出はローカル / 隔離環境に限定する。
