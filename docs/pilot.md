# MVPの試用受入

対象は自組織の開発者によるHTTP APIの試用です。作者以外の開発者による導入確認と、自動テストの結果を分けて記録します。

[2026-09-08の検証記録](pilot-validation.md)：機能・復元は成功、2時間の継続負荷は不合格です。

## 開発者へ渡すもの

管理者はバージョンを固定したCLI・ランタイム、管理APIのHTTPS URL、アプリ用ドメイン、テナント名、Read・Deployトークンを用意します。試用する開発者にkubeconfigは渡しません。Secretの登録・利用許可と外向き通信の承認はテナント管理者が担当します。

[認証付き在庫照会API](../sdk/examples/inventory-api/README.md)を最初の題材に使います。接続先はこのAPIの契約を満たすHTTPサービスです。自動試験ではテスト用の在庫を使います。実際の業務で役立つかどうかの検証には、利用者が必要とする接続先とデータを別途用意します。

## 作者以外の開発者による確認

以下は人による確認項目です。自動スクリプトの成功で代替しません。READMEとCLIガイドを渡し、口頭での補足が必要だった箇所を残してください。

| 作業 | 合格条件 | 結果 |
| --- | --- | --- |
| Honoの新規プロジェクトを作成 | `init`から`dev`まで手順書で実行できる | 未実施 |
| 初回配備 | 開発者トークンで配備し、自分のAPI URLを特定できる | 未実施 |
| 認証・Secrets | 管理者と開発者の作業を区別し、値をGitやログに入れず設定できる | 未実施 |
| 在庫照会 | 認証付きHTTPと期待する業務データを確認できる | 未実施 |
| 更新と復旧 | 新版へ更新し、問題があればCLIから旧版へ戻せる | 未実施 |

所要時間、実行環境、詰まった手順、想定と違った挙動を記録します。アプリの接続要件が現在のHibanaで扱えない場合も失敗として記録し、成功した題材だけで評価しません。

## 自動試験とリリース候補

`scripts/acceptance/kubernetes.py`はポート固定なしの専用kindを作り、明示したKubernetesコンテキストへCLIで導入します。CPとWorkerは内容に基づく同じイメージタグを使い、実PodのimageIDと、アプリの更新前後のWasm SHA-256を記録します。

コードに書いた値と環境変数を別々にHTTP応答へ返す小さなAPIも使い、更新と双方向のrollbackで両方が揃って切り替わることを確認します。Wasmのハッシュが異なるだけでは成功としません。

認証・Secrets・HTTP通信・不正入力・外部エラー・コードとvarsの更新・rollbackを確認後、並列4、各クライアントがリクエスト間に100ms待つ条件で2時間動かします。最大投入量は40req/s未満です。これは最大性能の測定ではありません。HTTPステータス・本文・版を検証し、30秒ごとに応答時間、Workerのメモリ・キャッシュ・再起動、DB容量・接続数・実行数を記録します。

HTTP失敗を検出すると負荷を停止し、既に送信したリクエストの完了を待ちます。負荷の不合格とバックアップ・復元の結果は別々に記録し、復元の成功で負荷の失敗を相殺しません。`--seconds 30`で機能・復元確認を短時間で再実行できますが、2時間の受入条件を満たしたとは扱いません。

保守後はPodのReadyを待ち、続いてアプリの`/health`を最大60秒確認します。新しいWorkerのコンパイル準備中に返る503と、アプリが応答できるまでの時間を記録します。その他の応答エラーや時間超過は失敗です。応答開始後に、在庫照会・Secrets・双方向のrollbackを改めて確認します。

合格にはHTTP失敗0、Worker再起動・置き換わり0、観測の欠落0、バックアップから一時DBへの復元・Secretsの実復号成功、保守後のAPIとrollbackの成功を必要とします。DB復元は稼働DBを上書きせず別DBへ実施します。S3オブジェクトはバックアップ時に取得してハッシュを確認しますが、この試験だけで別クラスタへの全体復旧やS3フェイルオーバーを実証したとはしません。

このkind試験は単一の物理ホスト上で動きます。実ノード喪失、強制力のあるCNIでの分離、TLS、複数台DB・Redis・S3の切替、実Ingress/LBでの迂回は、本番候補環境の別の受入条件です。

## 容量設定で注意する点

公開HTTPのレートと、Secret取得用の内部APIのレートは別の設定です。以前の既定値では公開HTTPは50req/s・burst 500、Secret取得は600回/分だったため、正常に受け付けたSecret付きAPIが負荷時に502になることを在庫APIの試験で確認しました。Secret取得の既定値を3,500回/分へ変更し、公開HTTPの既定レートとburstを扱えるようにしています。

`JOB_ENV_EXCHANGE_RATE_PER_MIN`はテナント単位と接続元IP単位の両方へ適用されます。既存環境で`600`を明示している場合は、その設定も更新してください。新しい既定値が使われるのは未指定の場合です。テナントのレートを増やしたり、多数のテナントを同じWorkerに収容したりする場合は、この内部予算も運用側で合わせます。設定の引き上げにはCP・DBの負荷検証が必要です。

実行記録はリクエストごとにDBへ保存され、現在は期限による自動削除を提供していません。試用期間とDB容量を決め、記録数・DB容量・バックアップ容量を監視します。2時間の成功だけで、容量を増やさず無期限に運用できるとは判断しません。継続運用へ進む前に、監査・利用量集計・Secrets参照との整合性を保った保存期間と削除手順を決める必要があります。
