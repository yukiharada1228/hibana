{
  "name": "@hibana/sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hibana SDK — write a Hono app, ship it as a WebAssembly Component",
  "bin": {
    "hibana": "src/cli.mjs"
  },
  "scripts": {
    "build:example": "node src/build.mjs --entry examples/hello.ts --out dist/hello.wasm"
  },
  "dependencies": {
    "@bytecodealliance/componentize-js": "^0.22.0",
    "@bytecodealliance/jco": "^1.32.1",
    "esbuild": "^0.24.0",
    "hono": "^4.6.0",
    "smol-toml": "^1.3.0"
  }
}
