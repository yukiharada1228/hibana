[package]
name = "hibana-http-app"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "=0.36.0"
serde_json = "1"

[profile.release]
opt-level = "s"
lto = true
strip = true

# Keep generated projects independent, including inside a larger Cargo workspace.
[workspace]
