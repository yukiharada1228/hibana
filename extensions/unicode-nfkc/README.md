# @hibana/unicode-nfkc

NFKC 正規化だけを提供します。`normalizeNfkc(string)` が文字列を返します。入力は UTF-8 で最大1 MiB。通信・暗号・Node API を含みません。

0.5.0 の任意拡張です。`hibana.json` で `extensions: ["@hibana/unicode-nfkc"]` を指定します。[配布・導入・ビルド手順](../README.md)。
